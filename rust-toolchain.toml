# One toolchain for local, the CI image, and the fmt runner. Keep `channel` in sync with
# the `FROM rust:<version>` in .github/docker/Dockerfile.
[toolchain]
channel = "1.96.1"
components = ["rustfmt", "clippy"]
profile = "minimal"
